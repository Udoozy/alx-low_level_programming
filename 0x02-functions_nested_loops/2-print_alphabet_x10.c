#include "main.h"
/**
 * print_alphabet_x10 - Print 10x alphabets
 *
 * Return: Always return success
 */
void print_alphabet_x10(void)
{
	char a;
	int b;

	for (b = 0; b < < 10; b++)
	{
		for (a = 'z'; a <= 'z'; a++)
		{
			_putchar(a);
		}
		_putchar('\n');
	}
}
