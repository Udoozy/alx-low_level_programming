#include "main.h"
/**
 * mul - Multiply two intergers
 * @x: First parameter
 * @y: Second Parameter
 * Return: Always return result
 */
int mul(int x, int y)
{
	int result = (x * y);

	return (result);
}
